<h1>Write A REVIEW!!!!</h1>

<%= simple_form_for ([@restaurant, @review]) do |f| %>
<%= f.input :content %>
<%= f.input :rating%>
<div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
